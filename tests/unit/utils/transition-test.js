import transition from 'dummy/utils/transition';
import { module, test } from 'qunit';

module('Unit | Utility | transition', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = transition();
    assert.ok(result);
  });
});
